const ConfirmedBooking = () => {
  return (
    <section>
      <div className="container" style={styles}>
        <h2>Booking confirmed</h2>
      </div>
    </section>
  );
};

const styles = {display: "grid", gridTemplateColumns: "auto", justifyContent: "center", margin: "4rem 0"}

export default ConfirmedBooking;
